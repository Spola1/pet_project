<% provide :page_title, "Tags" %>
<% currently_at 'Tags' %>

<h2>
  All questions with tag: <%= content_tag :span, "##{@tag.title}" %>
</h2>

<div class="mt-3">
  <%= pagination @pagy %>
</div>

  <%= render @questions %>

<div class="mt-3">
  <%= pagination @pagy %>
</div>
